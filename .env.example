# ─── Site ─────────────────────────────────────────────────────────────────────
# Canonical URL for this environment — no trailing slash
# localhost  → http://localhost:3000
# preview    → https://<branch>-zylen.vercel.app  (set per deployment in Vercel)
# prod       → https://zylen.tech
NEXT_PUBLIC_SITE_URL=https://zylen.tech
